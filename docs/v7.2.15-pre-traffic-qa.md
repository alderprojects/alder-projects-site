# v7.2.15 — pre-traffic QA checklist

Run this before posting links to Front Porch Forum, Reddit, or any local
homeowner group. The whole pass takes ~20 minutes if everything is green.

## Window flow

- [ ] `/guides/window-film-vs-replacement-vermont` renders. H1 + lead
      paragraph load above the fold.
- [ ] Three CTAs are visible (top inline, mid-section inline after "What
      the $19.99 Smart Cart actually does", bottom hero).
- [ ] Top CTA opens `CurationModal` prefilled with: topic
      `weatherization`, scope `window_weatherization`, scenario
      `just_starting`.
- [ ] DOM check via DevTools: every guide CTA has `data-topic-id`,
      `data-scope-id`, `data-scenario-id` matching the above.
- [ ] `/smart-cart/topic/window-weatherization-vermont` renders. The
      "Why $19.99?" module is visible above the skip section, with the
      window-specific copy ("$300+ on the wrong scope before they
      understood the difference between an air-leak window and a
      conductive-loss window").
- [ ] Topic page primary CTA opens `CurationModal` with the same prefill.
- [ ] Result page renders for a finished cart. Header shows: Built for /
      Project / Cart fee / Core cart / Optional add-ons / All-in if every
      pick / Avoided spend (if ≥ $50). The all-in number reconciles
      against core + optional. No line says "If you bought every pick" =
      core total.
- [ ] Sidebar (desktop) shows: Core cart, Optional add-ons (+range), All-
      in if every pick, Smart Cart fee, Avoided spend. The mobile bottom
      sheet shows the core total by default; expanding reveals the full
      breakdown.
- [ ] Result page route-out section ("When to skip this cart and call a
      pro") is below the buy list (window scope) and reads: "This cart is
      NOT the right tool for the cases below. If any of these apply, skip
      the cart and talk to a pro first."
- [ ] Savings line is split: "Overbuying on this project: $160–$460" and
      "Possibly deferring a major project: $1,000+" (window deferral
      copy explains the conditional).
- [ ] Checkout CTA fires.
- [ ] `/window-replacement-vermont` renders the bridge module above the
      service body. Bridge headline: "Before you replace, try $80–$220
      of weatherization." Italic secondary line points to the contractor
      list below.

## Basement flow

- [ ] `/guides/before-finishing-basement-moisture-checks-vermont` renders.
- [ ] Three CTAs visible. Top CTA opens `CurationModal` with topic
      `home_repair`, scope `basement_moisture_prep`, scenario
      `just_starting`.
- [ ] DOM check via DevTools: every guide CTA has the deterministic data
      attributes.
- [ ] "Why $19.99?" module is visible after the inline CTA, with the
      basement copy ("difference between a humidity problem and an
      intrusion problem before drywall goes up").
- [ ] `/smart-cart/topic/basement-moisture-prep` renders.
- [ ] Topic page primary CTA opens `CurationModal` with the same prefill.
- [ ] Result page renders. Route-out section ("When to skip this cart
      and call a pro") appears ABOVE the buy list.
- [ ] No copy implies active water or visible mold can be fixed by the
      cart. The route-out reasons name the specific failure modes:
      standing water, visible mold, foundation cracks/bowing, recurring
      dampness above 65%.
- [ ] Checkout CTA fires.
- [ ] `/basement-finishing-vermont` renders the bridge module. Bridge
      headline: "Before you finish the basement, check for moisture
      problems." Bridge has TWO CTAs side by side: the cart and a
      clickable contractor link reading "Have active water, mold, or
      structural issues? Talk to a basement waterproofing pro →".

## Smart Cart entry

- [ ] `/smart-cart` loads with the picker.
- [ ] Window weatherization is position 1 in the picker.
- [ ] Basement moisture prep is position 2.
- [ ] Coming-soon items render in a separate row beneath the active
      tiles, visually muted, NOT clickable.
- [ ] The example preview card defaults to "Window weatherization · Just
      starting" on first load.
- [ ] Selecting a different active tile updates: the example card
      teaser, the hero CTA's data attributes, and (for window/basement)
      the inline "Why $19.99?" module.
- [ ] The Worth-It line reads: "Bigger than a shopping list? Join the
      Worth-It waitlist for larger renovation decisions." It is visually
      secondary to the primary Build My Smart Cart CTA.

## SEO / source

- [ ] View source on `/guides/window-film-vs-replacement-vermont` and
      confirm the JSON-LD scripts: one `<script
      type="application/ld+json">` per Article, BreadcrumbList, FAQPage.
      No nesting. Each parses as valid JSON.
- [ ] Same on `/guides/before-finishing-basement-moisture-checks-vermont`.
- [ ] View source on
      `/smart-cart/topic/window-weatherization-vermont`. Confirm
      JSON-LD: Article, BreadcrumbList, Product (with Offer at
      $19.99). No fake `aggregateRating`. No `reviewCount`.
- [ ] Same on `/smart-cart/topic/basement-moisture-prep`.
- [ ] Sitemap (`/sitemap.xml`) includes all four target pages.
- [ ] No page emits `noindex` on these pages (`<meta
      name="robots">` and HTTP `X-Robots-Tag` both absent or set to
      index/follow).
- [ ] Canonical URLs match the actual path (no www, no trailing slash
      drift).
- [ ] Window guide and basement guide internally link to each other via
      `relatedGuideSlugs`.

## Analytics

Open the GA4 DebugView before clicking through.

- [ ] Visiting `/guides/window-film-vs-replacement-vermont` fires
      `guide_view` once.
- [ ] Visiting `/smart-cart/topic/window-weatherization-vermont` fires
      `topic_view` once.
- [ ] Clicking a guide CTA fires `smart_cart_cta_click` →
      `curation_modal_open` → `curation_modal_prefilled` with matching
      topic/scope/scenario.
- [ ] Clicking the bridge on `/window-replacement-vermont` fires
      `bridge_module_click`.
- [ ] Clicking the contractor secondary CTA on `/basement-finishing-
      vermont` fires `contractor_continue_click`.
- [ ] Loading a synthesized cart at `/smart-cart/result/[id]` fires
      `result_header_view` and `value_banner_view` (existing
      result-page events).
- [ ] Clicking the buy CTA fires
      `primary_cta_view_retailers_buy_click`.
- [ ] Clicking a `WhenToCallAPro` row fires `route_out_click`.

## Known-OK gaps

These are intentional and should not block traffic.

- The picker doesn't fire a per-tile `picker_select` event yet.
- Why-$19.99 module impressions are not tracked.
- The Worth-It waitlist link is a passive `Link` with no analytics
  attached — joining the waitlist is captured by the `/worth-it` page
  itself.
